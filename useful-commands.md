# Terminal 1 - Frontend:
  cd frontend
  npm run dev
  → http://localhost:3000

# Terminal 2 - Backend:
  cd backend
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt
  python main.py
  → http://localhost:8000